--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -41,6 +41,9 @@
    set(CMAKE_BUILD_TYPE RelWithDebInfo)
 endif()
 
+# Packaging: do not embed RPATH (old SetPaths.cmake RPATH block removed; keep disabled)
+set(CMAKE_SKIP_INSTALL_RPATH TRUE)
+
 include(CheckCCompilerFlag)
 include(CheckFunctionExists)
 
